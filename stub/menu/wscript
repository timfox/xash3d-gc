#! /usr/bin/env python
# encoding: utf-8

def options(opt):
	pass

def configure(conf):
	pass

def build(bld):
	if bld.env.DEST_OS != 'gamecube':
		return

	bld.stlib(
		source = bld.path.ant_glob('*.c'),
		target = 'menu',
		includes = ['.', '../../engine', '../../engine/common', '../../engine/client', '../../engine/platform', '../../public'],
		defines = ['ENGINE_DLL=1'],
		use = ['engine_includes', 'public', 'werror'],
		install_path = bld.env.LIBDIR,
	)
