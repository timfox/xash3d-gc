# G491 reach G201 delta diagnostic — 2026-08-02

- Parent commit: `26ea4269cf`
- Hypothesis: `FI->GameInfo` is null because the engine-side `FS_LoadGameInfo`
  wrapper only processes `vfs.cfg` and never calls the filesystem API's
  `LoadGameInfo` entry point.
- Changed files: `engine/common/filesystem_engine.c`,
  `engine/server/sv_main.c`.
- Build result: PASS. Fresh ELF, valid DOL, and smoke disc generated.
- Probe result: `.ai/logs/dolphin-probe-20260802-195808/`.
  `FI->GameInfo=0x80ff6218`; `SV_InitGame` entered; server path resolved to
  `server`; G201 executed.
- G201 evidence:
  `delta.lst`: `FS_FileExists=0`, `FS_LoadFile=0`, size `0`, disk `(none)`.
  `valve/delta.lst`: `FS_FileExists=1`, `FS_LoadFile=0`, size `0`, disk
  `(none)`.
  The disc staging still contains `xash3d/valve/delta.lst`.
- Probe failure after G201: `Host_ErrorInit: Could not load model
  maps/c0a0e.bsp from disk`.
- Keep/revert decision: keep the missing `g_fsapi.LoadGameInfo` handoff as a
  candidate fix because it reaches the requested boundary, but do not accept
  it for release. The runtime still has a filesystem load contract failure and
  a smoke-map staging failure.

## Next hypothesis

The active search paths recognize `valve/delta.lst` through `FS_FileExists`,
but `FS_LoadFile` cannot load it. Reproduce that mismatch for `maps/c0a0e.bsp`
and `delta.lst` using the existing independent diagnostics before changing
path normalization or asset staging.
