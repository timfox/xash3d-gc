# Overnight local-LLM brief (keep tiny)

Edit ONLY the loaded editable file(s). One small GameCube perf/memory patch.
Do NOT touch engine/server/*, create files, or delete functions.
Do NOT rewrite whole functions — one short SEARCH/REPLACE hunk only.
Prefer skipping redundant work (reports, scans, alloc churn) over refactors.
